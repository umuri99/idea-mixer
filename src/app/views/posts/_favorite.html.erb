<%= form_with(model: current_user.favorites.build, local: false, class: "favorite_form") do |f| %>
  <div><%= hidden_field_tag :post_id, @post.id %></div>
  <%= f.button type: "submit" do %>
    <div class="icon-favorite"></div>
  <% end %>
<% end %>
